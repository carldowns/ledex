package quote;

import cmd.BaseCmd;

/**
 * verifies that all of the necessary part of the quote are in place and valid
 */
public class QuoteVerificationHandler implements QuoteHandler {

    @Override
    public void evaluate(BaseCmd command) {

    }
}
