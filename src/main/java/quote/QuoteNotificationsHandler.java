package quote;

import cmd.BaseCmd;

/**
 * handles notifications that are sent as a result of a successful quote
 */
public class QuoteNotificationsHandler implements QuoteHandler {

    @Override
    public void evaluate(BaseCmd command) {

    }
}
