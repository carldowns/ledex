package quote;

/**
 * verifies that all of the necessary part of the quote are in place and valid
 */
public class QuoteVerifier implements QuoteHandler {

    @Override
    public void evaluate(BaseQuoteCmd command) {

    }
}
